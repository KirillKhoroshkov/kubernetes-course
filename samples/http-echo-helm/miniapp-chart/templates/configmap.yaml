apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "miniapp-chart.fullname" . }}-config
  labels:
    {{- include "miniapp-chart.labels" . | nindent 4 }}
data:
  MESSAGE: {{ .Values.config.message | quote }}
